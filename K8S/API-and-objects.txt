kubectl auth can-i create deployments
kubectl auth can-i create pods --as linda
kubectl auth can-i create pods --as <userid> --namespace <>

:) -> service+LB
                -> deployments{upgrades+replication}(pod:ip+volume -> \ )
                -> PVC(persistent volume claim) -> PV

kubectl api-resources
After running "kubectl proxy", you can use "curl" to explore group info. 
  -> curl http://localhost:8001/apis
 
kubectl api-versions ; kubectl explain # explain api components 

kubectl cluster-info
## .kube/config //current configuration
kubectl config view ## to view the above config. 

## Install bash-completion for kubectl, and enable it using the command below. 
kubectl completion bash > /etc/bash_completion.d/kubectl

kubectl -h ; kubectl cluster-info -h

kubectl explain pod.spec ## to list the fields used in the specification of Pod kind. 
  ## pod.{metadata,kind,apiVersion,spec}
kubectl explain pod.spec.containers ## list fields in container spec. 

========================================================================

etcd <- api-server <->
                    - kube-proxy -> curl(http) (here curl not takes care of TLS, so kube-proxy does)
                    - curl(https) -> TLS ## need to parse --cert, --key, --cacert values
                    - kubectl -> .kube/config(with TLS in it)
                    
kubectl proxy --port=8001 & ## '&' is important. 
curl http://localhost:8001/version 
curl http://localhost:8001/api/v1/namespaces/default/pods ## also pods/<pod_name>
========================================================================

etcdctl - To troubleshoot/manage etcd database
etcdctl is version independent, etcdctl2 to talk with v2 API. 

sudo yum provides */etcdctl
sudo yum install -y etcd 

etcdctl -h



